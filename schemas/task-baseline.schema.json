{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/CoaseEdge/repo-governance/schemas/task-baseline.schema.json",
  "title": "Repository task failure baseline",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "createdAt", "tests"],
  "properties": {
    "schemaVersion": { "const": 1 },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$"
    },
    "tests": {
      "type": "object",
      "propertyNames": { "$ref": "#/$defs/suiteId" },
      "additionalProperties": { "$ref": "#/$defs/suiteResults" }
    }
  },
  "$defs": {
    "suiteId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*$"
    },
    "suiteResults": {
      "type": "object",
      "additionalProperties": false,
      "required": ["failed"],
      "properties": {
        "failed": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 1,
            "pattern": "\\S"
          }
        }
      }
    }
  }
}
